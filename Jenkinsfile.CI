def run() {
    node('my-pipeline-CI'){
        stage('CI Stage') {
            echo 'I am the first CI'
        }
        stage('CI Stage 2') {
            echo 'I am the second CI'
        }
	}
}

return this